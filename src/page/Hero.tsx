import React from "react";
import { personalInfo } from "@/data/mock";
const Hero = () => {
  return (
    <section id="home" className="hero">
        <p>hero</p>
    </section>
    );
};

export default Hero;